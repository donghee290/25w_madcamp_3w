using UnityEngine;

public class PropOffset : MonoBehaviour
{
    public float yOffset = 0f;      // 위로 올릴 값
    public float zOffset = 0f;      // 벽에서 더/덜 붙일 값 (선택)
    public float alongOffset = 0f;  // slot.right 방향으로 미세 조정 (선택)
}